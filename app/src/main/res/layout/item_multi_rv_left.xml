<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="com.niukeclient.niuke.ui.viewModel.homeViewModel.MultiRecycleLeftItemViewModel" />

        <variable
            name="viewModelLeft"
            type="com.niukeclient.niuke.ui.viewModel.homeViewModel.MultiRecycleLeftItemViewModel" />

    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:binding="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:padding="10dp"
        binding:onClickCommand="@{viewModelLeft.itemClick}">
        <GridView
            android:gravity="center"
            android:layout_margin="8dp"
            android:id="@+id/grid_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:numColumns="4" />

    </FrameLayout>
</layout>